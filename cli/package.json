{
  "name": "@dep/cli",
  "version": "0.1.0",
  "description": "CLI tool for the Documentation Engineering Protocol",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "dep": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run scripts/build.ts",
    "build:local": "bun build --compile ./src/index.ts --outfile dist/dep",
    "test": "bun test"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0",
    "gray-matter": "^4.0.3",
    "yaml": "^2.8.3"
  }
}
