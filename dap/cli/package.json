{
  "name": "@dap/cli",
  "version": "0.1.0",
  "description": "CLI tool for the Decision Action Protocol",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "dap": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build:local": "bun build --compile ./src/index.ts --outfile dist/dap",
    "test": "bun test"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "yaml": "^2.8.3"
  }
}
